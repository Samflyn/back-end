package abstractuse;

//abstract class does not allow to create an object but allows to declare reference.
public abstract class Sample {
	public void display() {

	}

	public abstract void show();
	// Class should be abstract even if one method is abstract.
}